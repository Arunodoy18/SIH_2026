"""Trained ML models. Currently: landslide susceptibility (RandomForest).

See features.py (shared feature computation), generate_landslide_inventory.py (synthetic
training points — swap for a real GSI/Bhukosh inventory), train_landslide_model.py, and
landslide_ml.py (inference, with a clean fallback when no model is trained yet).
"""
